doctype html
html
  head
    include head
  body
    include header
    main.main
      block main
    footer.foot
      div.foot-copy.u-fl
        != '&copy; ' + date(new Date(), 'YYYY') + ' ' + (config.author || config.title)
        if theme.themes_mata
          bdi
            != theme.themes_mata
      block menu
    include footer