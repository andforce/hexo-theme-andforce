header.head
    h1.head-title.u-fl: a(href = config.root)
        = config.title
    nav.head-nav.u-fr
        ul.head-nav__list
            each val, index in theme.menu
                li.head-nav__item: a.head-nav__link(href = val)= index