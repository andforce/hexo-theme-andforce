include _partial/post
include _partial/menu
extends _partial/main

block main
  -page.posts.each(function(post){
    +post(post, 'category')
  -})

block head
  title
    = page.category + ' - ' + config.title

block menu
  +menu('category')